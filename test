<?php
include('../config/connect.php'); // เชื่อมต่อฐานข้อมูล

// ดึง student_id จากเซสชัน
if (isset($_SESSION['student'])) {
    // ดึง student_id จากเซสชัน
    $student_id = $_SESSION['student'];

    // ดึงข้อมูล student จากฐานข้อมูล
    $query = "SELECT username FROM student WHERE student_id = :student_id";
    $stmt = $condb->prepare($query);
    $stmt->bindParam(':student_id', $student_id, PDO::PARAM_INT);
    $stmt->execute();
    $student = $stmt->fetch(PDO::FETCH_ASSOC);

} else {
    echo "Student ID is not set in the session.";
}
?>


<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>เปลี่ยนรหัสผ่าน</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome CSS -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet">
    <!-- SweetAlert2 CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.css">
    <!-- Custom CSS -->
    <style>
    /* Card styles */
    .card-custom {
        border-color: #007bff;
        /* Primary blue border color */
        border-radius: 0.5rem;
        /* Rounded corners */
        box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
        /* Light shadow */
        margin-bottom: 1.5rem;
        /* Space between cards */
    }

    .card-header-custom {
        background-color: #000000;
        /* Black background */
        color: #fff;
        /* White text */
        border-bottom: 1px solid #000000;
        /* Border color */
    }

    .form-control {
        border-radius: 0.25rem;
        /* Rounded corners */
        box-shadow: none;
        /* Remove box shadow */
    }

    .form-group label {
        font-weight: bold;
        /* Bold labels */
    }

    .form-group .form-control {
        width: 100%;
        /* Full width */
    }

    /* Custom button styles */
    .btn-info {
        background-color: #17a2b8;
        /* Greenish-blue */
        border-color: #17a2b8;
        /* Greenish-blue */
    }

    .btn-info:hover {
        background-color: #138496;
        /* Darker greenish-blue */
        border-color: #117a8b;
        /* Darker greenish-blue */
    }

    .text-primary {
        color: #007bff;
        /* Primary blue */
    }

    /* Form advice */
    .form-advice ul {
        list-style-type: disc;
        /* Bullet points */
        padding-left: 1.5rem;
        /* Indent list */
    }

    .card-body-custom {
        display: flex;
        align-items: flex-start;
    }

    .form-container {
        flex: 2;
        margin-right: 10rem;
        max-width: 500px;
    }

    .advice-container {
        flex: 1;
        margin-right: 1rem;
    }

    .advice-card {
        border-color: #007bff;
        /* Primary blue border color */
        border-radius: 0.5rem;
        /* Rounded corners */
        box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
        /* Light shadow */
        margin-bottom: 1.5rem;
        /* Space between cards */
    }

    .advice-card .card-header {
        background-color: #6c757d;
        /* Gray background */
        color: #fff;
        /* White text */
    }
    </style>
</head>

<body>
    <!-- Content Wrapper. Contains page content -->
    <div class="content-wrapper">
        <!-- Content Header (Page header) -->
        <section class="content-header">
            <div class="container-fluid">
                <div class="row mb-2">
                    <div class="col-sm-6">
                        <h1 class="text-primary">เปลี่ยนรหัสผ่านหรือชื่อผู้ใช้</h1>
                    </div>
                </div>
            </div><!-- /.container-fluid -->
        </section>

        <!-- Main content -->
        <section class="content">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-md-12">
                        <!-- Change Password Form Card -->
                        <div class="card card-custom">
                            <div class="card-header card-header-custom">
                                <h3 class="card-title">ฟอร์มเปลี่ยนรหัสผ่าน</h3>
                            </div>
                            <div class="card-body card-body-custom">
                                <!-- Form Container -->
                                <div class="form-container">
                                    <form id="changePasswordForm">
                                        <div class="form-group">
                                            <label for="username">ชื่อผู้ใช้</label>
                                            <input type="text" class="form-control" id="username" name="username"
                                                value="<?php echo $student['username']; ?>" required>
                                        </div>

                                        <div class="form-group">
                                            <label for="currentPassword">รหัสผ่านปัจจุบัน</label>
                                            <div class="position-relative">
                                                <input type="password" class="form-control" id="currentPassword"
                                                    name="currentPassword" required>
                                                <i class="fa fa fa-eye-slash position-absolute"
                                                    id="toggleCurrentPassword"
                                                    onclick="togglePassword('currentPassword', 'toggleCurrentPassword')"
                                                    style="right: 10px; top: 50%; transform: translateY(-50%); cursor: pointer;"></i>
                                            </div>
                                        </div>

                                        <div class="form-group">
                                            <label for="newPassword">รหัสผ่านใหม่</label>
                                            <div class="position-relative">
                                                <input type="password" class="form-control" id="newPassword"
                                                    name="newPassword" required>
                                                <i class="fa fa-eye-slash position-absolute" id="toggleNewPassword"
                                                    onclick="togglePassword('newPassword', 'toggleNewPassword')"
                                                    style="right: 10px; top: 50%; transform: translateY(-50%); cursor: pointer;"></i>
                                            </div>
                                        </div>

                                        <div class="form-group">
                                            <label for="confirmNewPassword">ยืนยันรหัสผ่านใหม่</label>
                                            <div class="position-relative">
                                                <input type="password" class="form-control" id="confirmNewPassword"
                                                    name="confirmNewPassword" required>
                                                <i class="fa fa-eye-slash position-absolute" id="toggleConfirmPassword"
                                                    onclick="togglePassword('confirmNewPassword', 'toggleConfirmPassword')"
                                                    style="right: 10px; top: 50%; transform: translateY(-50%); cursor: pointer;"></i>
                                            </div>
                                        </div>

                                        <button type="submit" class="btn btn-info">เปลี่ยนรหัสผ่าน</button>
                                    </form>
                                </div>
                                <!-- Advice Container -->
                                <div class="advice-container">
                                    <div class="card advice-card">
                                        <div class="card-header">
                                            <h4 class="card-title">คำแนะนำในการเปลี่ยนรหัสผ่าน</h4>
                                        </div>
                                        <div class="card-body">
                                            <ul>
                                                <li>ชื่อผู้ใช้: สามารถแก้ไขชื่อผู้ใช้ได้จากชื่อผู้ใช้ปัจจุบัน</li>
                                                <li>รหัสผ่านปัจจุบัน: กรุณากรอกรหัสผ่านที่ใช้เข้าสู่ระบบปัจจุบัน</li>
                                                <li>รหัสผ่านใหม่: ควรมีความยาวอย่างน้อย 8 ตัวอักษร ประกอบด้วย a-z,A-Z,0-9</li>
                                                <li>ยืนยันรหัสผ่านใหม่: กรุณากรอกรหัสผ่านใหม่อีกครั้งเพื่อยืนยัน</li>
                                            </ul>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </div>
    <!-- /.content-wrapper -->

    <!-- jQuery, Popper.js, and Bootstrap JS -->
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <!-- SweetAlert2 JS -->
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
    document.getElementById('changePasswordForm').addEventListener('submit', function(event) {
        event.preventDefault(); // Prevent form submission

        const formData = new FormData(this);
        const username = formData.get('username');
        const currentPassword = formData.get('currentPassword');
        const newPassword = formData.get('newPassword');
        const confirmNewPassword = formData.get('confirmNewPassword');

        const passwordRegex = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)[a-zA-Z\d]{8,}$/;

        if (!passwordRegex.test(newPassword)) {
            Swal.fire({
                title: 'ข้อผิดพลาด!',
                text: 'รหัสผ่านต้องประกอบด้วย a-z,A-Z,0-9 และมีความยาวอย่างน้อย 8 ตัวอักษร',
                icon: 'error',
                confirmButtonText: 'ตกลง'
            }).then(() => {
                document.getElementById('newPassword').value = '';
                document.getElementById('confirmNewPassword').value = '';
            });
            return;
        }

        if (newPassword !== confirmNewPassword) {
            Swal.fire({
                title: 'ข้อผิดพลาด!',
                text: 'รหัสผ่านใหม่และการยืนยันรหัสผ่านใหม่ไม่ตรงกัน',
                icon: 'error',
                confirmButtonText: 'ตกลง'
            }).then(() => {
                // ล้างค่าของช่องกรอก รหัสผ่านใหม่ และ ยืนยันรหัสผ่านใหม่
                document.getElementById('newPassword').value = '';
                document.getElementById('confirmNewPassword').value = '';
            });
            return;
        }

        // AJAX request to handle password change
        $.ajax({
            url: '../fetch/change_password_process_student.php', // URL to the PHP script
            type: 'POST',
            data: formData,
            processData: false,
            contentType: false,
            success: function(response) {
                console.log('Response:', response); // Log response for debugging
                const result = JSON.parse(response);
                if (result.status === 'success') {
                    Swal.fire({
                        title: 'สำเร็จ!',
                        text: "รหัสผ่านถูกเปลี่ยนเรียบร้อยแล้ว",
                        icon: 'success',
                        confirmButtonText: 'ตกลง'
                    }).then(() => {
                        window.location.href = '../loginregis.php';
                    });
                } else {
                    Swal.fire({
                        title: 'ข้อผิดพลาด!',
                        text: result.message,
                        icon: 'error',
                        confirmButtonText: 'ตกลง'
                    }).then(() => {
                        // ล้างค่าของทุกช่องกรอกเมื่อเกิดข้อผิดพลาดอื่น ๆ
                        document.getElementById('currentPassword').value = '';
                    });
                }
            },
            error: function(xhr, status, error) {
                console.error('AJAX Error:', status, error); // Log AJAX errors for debugging
                Swal.fire({
                    title: 'ข้อผิดพลาด!',
                    text: "มีข้อผิดพลาดเกิดขึ้นในการเปลี่ยนรหัสผ่าน",
                    icon: 'error',
                    confirmButtonText: 'ตกลง'
                }).then(() => {
                    // ล้างค่าของทุกช่องกรอกเมื่อเกิดข้อผิดพลาดอื่น ๆ
                    document.getElementById('currentPassword').value = '';
                    document.getElementById('newPassword').value = '';
                    document.getElementById('confirmNewPassword').value = '';
                });
            }
        });
    });

    function togglePassword(fieldId, toggleId) {
        var field = document.getElementById(fieldId);
        var toggleIcon = document.getElementById(toggleId);

        if (field.type === "password") {
            field.type = "text";
            toggleIcon.classList.remove('fa-eye-slash');
            toggleIcon.classList.add('fa-eye');
        } else {
            field.type = "password";
            toggleIcon.classList.remove('fa-eye');
            toggleIcon.classList.add('fa-eye-slash');
        }
    }
    </script>
</body>

</html>
